---
description: When working within a feature folder, create subfolders for
  components, hooks, types, and api to organize code.
alwaysApply: false
---

Structure each feature folder with subfolders for components, hooks, types, and api. For example, src/features/profile/components, src/features/profile/hooks, src/features/profile/types, and src/features/profile/api.